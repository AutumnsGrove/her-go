kind: worker_briefing
# No cron — briefings are on-demand only via send_task(target=worker).
# To enable daily automatic briefings, add: cron: "0 7 * * *"
cron: ""
payload: {}
retry:
  max_attempts: 2
  backoff: linear
  initial_wait: 2m
