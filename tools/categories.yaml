# Category hints for the deferred-tools table in main_agent_prompt.md.
# Each category has a "hint" that tells the agent when to load it.
# This is the single source of truth — the table in main_agent_prompt.md
# is auto-generated from this file at prompt load time.

vision:
  hint: "User sent a photo"

search:
  hint: "User asks about current events, facts you don't know, a specific book/author, or shares a link to read"

context:
  hint: "User asks about weather, mentions the outdoors/commute, or wants to set their home location"

calendar:
  hint: "User mentions a work shift, schedule, calendar event, or asks about hours worked"
